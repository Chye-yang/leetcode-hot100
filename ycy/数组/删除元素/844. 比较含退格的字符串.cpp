#include <iostream>
#include <string>
#include <vector>

using namespace std;


class Solution {
    public:
        bool backspaceCompare(string s, string t) {
            s=='#'
            int fast=0,slow=0;
            for(fast=0;fast<s.length;fast++){
                
                if(s[])

            }
        }
    };